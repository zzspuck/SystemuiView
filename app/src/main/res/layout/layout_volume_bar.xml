<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/layout_volume_bar"
    android:layout_width="180dp"
    android:layout_height="70dp"
    android:gravity="center_vertical"
    android:layerType="hardware"
    android:padding="10dp"
    android:background="@drawable/shape_bg_shortcut_panel_button_nor2"
    >

    <ImageView
        android:id="@+id/iv_volume_type"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_centerVertical="true"
        android:layout_marginStart="@dimen/dp114"
        android:src="@drawable/level_list_system_volume_bar"
        tools:ignore="ContentDescription" />

</RelativeLayout>